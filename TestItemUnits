SELECT STOCK.ITMREF_0, SUM(QTYSTU_0 / PUUSTUCOE_0) 
FROM x3v12.X.STOCK 
INNER JOIN x3v12.X.ITMMASTER ON ITMMASTER.ITMREF_0 = STOCK.ITMREF_0 
WHERE STOFCY_0 = --FACILITY
GROUP BY STOCK.ITMREF_0
HAVING SUM(QTYSTU_0 / PUUSTUCOE_0) <> FLOOR(SUM(QTYSTU_0 / PUUSTUCOE_0))
